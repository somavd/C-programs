/*
	Pointer to Array
*/
#include"lib.c"
void main()
{
int i=0,a[]={1,2,3,4,5,6,7,8,9};//a is integer array
int *p;							//p is pointer of datatype integer
clrscr();
p=a;						    //p points to array
for(i=0;*p!='\0';i++,p++)		//p incremented at each iteration
printf("\n%d",*p);
printf("\nEnter any kye to Exit");
getch();
}
